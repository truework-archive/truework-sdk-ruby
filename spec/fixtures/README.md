# Use these fixtures

```
# initial:
git subtree add --squash -P path/to/test/fixtures git@github.com:truework/truework-sdk-fixtures master

# to update:
git subtree merge --prefix=path/to/test/fixtures --squash master

```
